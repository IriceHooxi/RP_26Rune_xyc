# RP-26Rune 从零部署指南（git clone → run_demo）

本文档记录**在一台全新 Ubuntu 22.04 x86-64 机器上，从 `git clone` 开始到跑通无硬件 demo 的完整步骤**。

> [!IMPORTANT]
> 上游仓库当前版本（`7181abb`）**缺少两个编译期依赖**，直接执行 `./run_demo.sh` 会在编译阶段失败：
> 1. flatbuffers `25.2.10` 头文件（`*_generated.h` 有精确版本 `static_assert`）；
> 2. Foxglove C 静态库 `libfoxglove.a`（仓库只 vendored 了 C++ 包装层源码）。
>
> 因此第 4、5 步是**必需的补齐步骤**，不是可选优化。建议向上游仓库反馈，把这两项纳入
> `scripts/setup_ubuntu22.sh`。

## 0. 步骤总览

| # | 步骤 | 关键命令 | 耗时 | 需联网 |
| --- | --- | --- | --- | --- |
| 1 | 环境要求（核对，无操作） | — | — | — |
| 2 | 克隆仓库 | `git clone` | 秒级 | 是 |
| 3 | 系统依赖 | `./scripts/setup_ubuntu22.sh` | 5–15 min（OpenVINO + Sophus） | 是 |
| 4 | 补 flatbuffers 头文件 | `git clone -b v25.2.10` + `cp` | 秒级 | 是 |
| 5 | 补 `libfoxglove.a` | Rust 工具链 + `cargo build -p foxglove_c` | 3–6 min | 是 |
| 6 | 构建工程 | `cmake -S . -B build && cmake --build build` | 3–6 min | 否 |
| 7 | 运行 demo | `./run_demo.sh [--headless]` | — | 否 |

磁盘预留：仓库 + 构建产物 + Rust 工具链 + SDK 源码，合计约 **2–3 GB**。

## 1. 环境要求

| 项目 | 要求 | 本指南验证环境 |
| --- | --- | --- |
| 操作系统 | Ubuntu 22.04 x86-64，具备 `sudo` | Ubuntu 22.04.5（容器无 sudo；依赖已预装，第 3 步仅做版本核对） |
| 编译工具链 | GCC 11+（C++20）、CMake 3.22+ | GCC 11.4.0 / CMake 3.22.1 |
| 视觉与优化库 | OpenCV 4.5.4+、Eigen3、Ceres、Boost、glog | OpenCV 4.5.4 / Ceres 2.0.0 / Boost 1.74 / glog 0.4.0 |
| 推理后端 | OpenVINO 2024.x（默认 CPU 插件） | OpenVINO 2024.6.0 |
| 位姿库 | Sophus 1.22.10 | Sophus 1.22.10（`/usr/local`） |
| 视频解码 | FFmpeg（OpenCV 可调用） | FFmpeg 4.4.2 |
| Rust 工具链 | ≥ 1.83（仅用于构建 `libfoxglove.a`） | rustc 1.98.1 |
| 显示环境 | 有桌面跑 GUI，无桌面用 `--headless` | `DISPLAY=:1`（X11 可用） |

> 若系统依赖已装好（例如复用战队已有开发机），可跳过第 2 步的安装，只执行
> `./scripts/setup_ubuntu22.sh --check` 做校验。

## 2. 克隆仓库

```bash
git clone https://github.com/SZURPVision/RP-26Rune.git
cd RP-26Rune
export REPO="$PWD"     # 后续步骤统一使用
```

## 3. 安装系统依赖（官方脚本）

```bash
./scripts/setup_ubuntu22.sh
```

脚本做的事（[setup_ubuntu22.sh](scripts/setup_ubuntu22.sh)）：

- `apt-get install`：`build-essential`、`cmake`、`pkg-config`、`libopencv-dev`、`ffmpeg`、
  `libeigen3-dev`、`libceres-dev`、`libgflags-dev`、`libboost-all-dev`、`libgoogle-glog-dev` 等；
- 配置 Intel 官方 APT 源并安装 `libopenvino-dev-2024.4.0` + CPU/AUTO 插件
  （可用 `OPENVINO_VERSION=2024.6.0 ./scripts/setup_ubuntu22.sh` 覆盖）；
- 从源码构建安装 Sophus `1.22.10`（可用 `SOPHUS_VERSION` 覆盖）；
- 校验随仓模型、视频、配置文件，并做一次干净的 CMake 配置 + 解码一帧 H.264 视频。

只检查不改系统：

```bash
./scripts/setup_ubuntu22.sh --check
```

**验证点**：脚本最后输出 `Environment check passed`，并打印 CMake / 编译器版本。

## 4. 补齐依赖 A：flatbuffers 25.2.10 头文件

**原因**：`src/core/utility/foxglove/autogenerated_flatbuffers/` 下 43 个 `*_generated.h` 都由
flatc 生成，文件头部有精确版本断言：

```cpp
static_assert(FLATBUFFERS_VERSION_MAJOR == 25 &&
              FLATBUFFERS_VERSION_MINOR == 2 &&
              FLATBUFFERS_VERSION_REVISION == 10,
             "Non-compatible flatbuffers version included");
```

Ubuntu 22.04 源里的 `libflatbuffers-dev` 是 2.0.0，**版本不匹配，必须用 25.2.10**。

```bash
git clone --depth 1 --branch v25.2.10 https://github.com/google/flatbuffers.git /tmp/flatbuffers
cp -r /tmp/flatbuffers/include/flatbuffers \
      "$REPO/src/core/utility/foxglove/autogenerated_flatbuffers/"
```

**验证点**（输出应为 25 / 2 / 10）：

```bash
grep -E "FLATBUFFERS_VERSION_(MAJOR|MINOR|REVISION)" \
  "$REPO/src/core/utility/foxglove/autogenerated_flatbuffers/flatbuffers/base.h"
```

> 该目录是 `foxglove_interface_lib` 的 include 路径，因此 `#include "flatbuffers/flatbuffers.h"`
> 与 `#include "Time_generated.h"` 都能解析。

## 5. 补齐依赖 B：`libfoxglove.a`

**原因**：[foxglove/CMakeLists.txt](src/core/utility/foxglove/CMakeLists.txt) 第 12 行要求链接
`${foxglove_SOURCE_DIR}/lib/libfoxglove.a`，但仓库只 vendored 了 C++ 包装层（`src/`、`include/`），
底层 C/Rust 静态库没有随仓提供。该静态库必须来自**与 vendored 包装层完全相同的上游快照**：

| 来源 | 版本 |
| --- | --- |
| 上游仓库 | [foxglove/foxglove-sdk](https://github.com/foxglove/foxglove-sdk) |
| 匹配快照 | commit `1f42d24640dc22ced5d21d19c971b448e750d274` |
| Rust crate | `foxglove_c`（`[lib] name = "foxglove"`，`crate-type = ["staticlib", "cdylib"]`） |

### 5.1 安装 Rust 工具链（≥ 1.83）

```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --profile minimal
source "$HOME/.cargo/env"
rustc --version      # 需 ≥ 1.83
```

> 无 `$HOME` 写权限时，可改为安装到任意可写目录：
> `export RUSTUP_HOME=<dir>/rustup CARGO_HOME=<dir>/cargo` 后再执行上面的安装命令。

### 5.2 拉取匹配快照

```bash
git clone --depth 1 https://github.com/foxglove/foxglove-sdk.git /tmp/foxglove-sdk
cd /tmp/foxglove-sdk
git fetch --unshallow
git checkout 1f42d24640dc22ced5d21d19c971b448e750d274
```

> 备选（下载量更小，但依赖 GitHub 允许按 SHA 抓取，网络不稳定时容易失败）：
> `git init && git remote add origin <url> && git fetch --depth 1 origin <sha> && git checkout FETCH_HEAD`
>
> 若上游更新了 `src/core/utility/foxglove/src/`，快照 commit 会变化，重新定位方法见附录 B。

### 5.3 构建静态库

```bash
export BINDGEN_EXTRA_CLANG_ARGS="-I$(gcc -print-file-name=include)"
cargo build --release -p foxglove_c
```

- 首次构建会下载并编译 `mcap`、`rustls`、`aws-lc-sys`、`tokio-tungstenite` 等依赖，约 3–6 分钟。
- `BINDGEN_EXTRA_CLANG_ARGS` 用于修复 `aws-lc-sys` 的 bindgen 报错
  `fatal error: 'stddef.h' file not found`（系统只装了 `libclang1-14`、没有 clang 自带头文件时必现）。
  若安装了完整的 clang，本行可省略。

### 5.4 放到工程期望位置

```bash
mkdir -p "$REPO/src/core/utility/foxglove/lib"
cp /tmp/foxglove-sdk/target/release/libfoxglove.a "$REPO/src/core/utility/foxglove/lib/"
```

**验证点**：文件存在且约 65 MB。

```bash
ls -lh "$REPO/src/core/utility/foxglove/lib/libfoxglove.a"
```

> `*.a` 已被 `.gitignore` 忽略，不会误提交；但换机器/清构建时需要重新放置或重新构建。

## 6. 构建工程

```bash
cd "$REPO"
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build --parallel "$(nproc)"
```

- 内存较小（≤ 8 GB）时把并行度降到 4–8，避免 OOM。
- 也可以直接跑 `./run_demo.sh`，它会自动完成"构建 + 启动"（并行度由 `BUILD_JOBS` 控制，默认 2）。

**验证点**：`output/` 下生成 `app` 与 14 个 `.so`：

```bash
ls output/app && ls output/*.so | wc -l    # app；14
```

关键产物：`libpower_rune_lib.so`（核心算法）、`libNNdetect_lib.so`（OpenVINO 推理）、
`libsingle_camera_manager_plugin_lib.so`（视频回放）、`libtracker_manager_plugin_lib.so`
（调用 power_rune 接口）、`libfire_control_system_plugin_lib.so`（火控）、
`libfoxglove_lib.so` / `libfoxglove_viz.so`（可视化）。

## 7. 运行 demo

```bash
# 有桌面环境：弹 OpenCV 调试窗口
./run_demo.sh

# 无桌面 / SSH：QT_QPA_PLATFORM=offscreen，不建窗口
./run_demo.sh --headless
```

按 `Ctrl+C` 退出（demo 视频默认 `loop=1`，会一直循环播放）。

运行时的数据链路（配置见 [config/runtime_config.json](config/runtime_config.json)）：

```text
ReceiveDecoder(模拟电控, 20ms) ─┐
CameraManager(回放 big_rune_demo.mp4, 30fps) → NNDetector(OpenVINO 五关键点)
   → TrackerManager(power_rune::process_power_rune) → PlannerControl(get_rune_data, 火控)
   → SendEncoder(模拟发送)
```

可视化出口：
- OpenCV 窗口：`RuneDemo/EndToEnd`，以及 `config/power_rune.json` 中 `debug` 开关控制的
  `PowerRune/Contours`、`PowerRune/NNResult` 等；
- Foxglove WebSocket：由 `*_FOXGLOVE` 系列开关控制（符平面向量、相位、预测误差、期望 yaw/pitch），
  用 Foxglove Studio 连接后看 Plot / 3D 面板，详见
  [foxglove_viz/README.md](src/core/utility/foxglove_viz/README.md)。

## 8. 跑通判据

启动数十秒内应看到以下日志（`glog` 同时输出到 stderr）：

```text
[NNDetector] initialized OpenVINO rune model; model=".../model-0624.onnx", device=CPU,
             input=640x480, output_layout=[N,C,A], anchors=6300, conf=0.65, kconf=0.5, nms=30, min_valid_kpts=5
context构建完成
[app] context initialized; plugin thread groups are running
[ReceiveDecoder] simulated ECS enabled: mode=BigRune, color=Red, ready=1, period_ms=20
[CameraManager] replay opened: .../big_rune_demo.mp4, fps=30, loop=1, eof_pause_ms=6000
[TrackerManager] 26-style TFTree initialized; camera_offset_m=(-0.1285, 0, 0.0441)
[BigRunePhaseMotionFilter]顺时针
```

最后一行 `[BigRunePhaseMotionFilter]顺时针` 表示整条链路已打通：视频 → 关键点推理 → 轮廓精修 →
三维位姿解算 → 相位提取 → 大符运动模型确认旋向。

## 9. 常见问题

| 现象 | 原因 | 处理 |
| --- | --- | --- |
| `没有规则可制作目标 "../src/core/utility/foxglove/lib/libfoxglove.a"` | 第 5 步未做 | 按 5.1–5.4 构建并放置 `libfoxglove.a` |
| `fatal error: flatbuffers/flatbuffers.h: 没有那个文件或目录` | 第 4 步未做 | 拷贝 flatbuffers 头文件 |
| `static_assert ... Non-compatible flatbuffers version included` | flatbuffers 版本不对 | 必须是 **25.2.10**，不要用 apt 的 2.0.0 |
| `fatal error: 'stddef.h' file not found`（cargo 构建 `aws-lc-sys` 时） | bindgen 找不到 C 头文件 | `export BINDGEN_EXTRA_CLANG_ARGS="-I$(gcc -print-file-name=include)"` |
| `error inheriting <dep> from workspace root manifest` / manifest 解析失败 | foxglove-sdk 源码树不完整 | 用 `git checkout -f <sha>` 强制切到快照，确保工作区干净 |
| `[glog] failed to create log dir: "../logs"` | glog 日志目录固定为**进程 CWD 的上一级**，父目录只读 | 非致命（`alsologtostderr=1`，日志仍打印在终端）；需要文件日志就把仓库放在可写目录下 |
| CMake 找不到 OpenVINO | 未装或未设置路径 | 跑 `scripts/setup_ubuntu22.sh`，或显式指定安装目录下的 cmake，如 `-DOpenVINO_DIR=/opt/intel/openvino_2024.6.0/runtime/cmake` |
| 找不到 `Sophus` | Ubuntu 22.04 无对应开发包 | 由 `setup_ubuntu22.sh` 从源码安装 1.22.10 |
| `imshow` 报错 / 无窗口 | 无 X11 | 用 `./run_demo.sh --headless` |
| 链接 `libfoxglove_lib.so` 时内存/时间暴涨 | 静态库约 65 MB | 降低并行度，耐心等待；不要中断链接过程 |
| SSH 断开后进程被杀 | 前台运行 | 用 `tmux`/`nohup`，退出用 `Ctrl+C` 或 `pkill -f output/app` |

## 10. 附录

### A. 为什么版本必须精确

| 组件 | 约束 | 依据 |
| --- | --- | --- |
| flatbuffers | `25.2.10` | 43 个 `*_generated.h` 的 `static_assert` |
| `libfoxglove.a` | 必须与 vendored C++ 包装层同一快照 `1f42d246` | 包装层直接调用 C ABI（`foxglove-c/foxglove-c.h`），跨版本结构体布局会变 |
| `include/foxglove-c/foxglove-c.h` | 随仓提供，已与快照一致 | 由上游 `c/build.rs` 用 cbindgen 生成 |
| Rust | ≥ 1.83 | `Cargo.toml` 的 `rust-version` |

### B. 上游更新后如何重新定位快照 commit

以随仓 `src/core/utility/foxglove/` 的实际文件为准，在 foxglove-sdk 历史中反查：

```bash
git clone --depth 1 https://github.com/foxglove/foxglove-sdk.git /tmp/foxglove-sdk
cd /tmp/foxglove-sdk && git fetch --unshallow

# 1) 先看某个特征文件在历史中的增删点，缩小候选范围
git log --format="%h %ad %s" --date=short -- cpp/foxglove/src/cloud_sink.cpp | head

# 2) 对候选 commit 与随仓源码做整树 diff，完全一致即为匹配快照
git archive <sha> cpp/foxglove/src | tar -x -C /tmp/check
diff -r /tmp/check/cpp/foxglove/src "$REPO/src/core/utility/foxglove/src" && echo MATCH

# 3) 用 C 头文件复核（应完全一致）
git show <sha>:c/include/foxglove-c/foxglove-c.h | diff - "$REPO/src/core/utility/foxglove/include/foxglove-c/foxglove-c.h"
```

### C. 一键脚本（可选）

把第 4、5 步保存为 `scripts/setup_foxglove_deps.sh`，之后新机器上"clone → 跑脚本 → run_demo"即可：

```bash
#!/usr/bin/env bash
set -Eeuo pipefail

REPO="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)"
FOXGLOVE_SDK_COMMIT="1f42d24640dc22ced5d21d19c971b448e750d274"
FLATBUFFERS_TAG="v25.2.10"
WORK_DIR="$(mktemp -d)"
trap 'rm -rf -- "${WORK_DIR}"' EXIT

echo "[1/4] flatbuffers ${FLATBUFFERS_TAG} headers"
git clone --depth 1 --branch "${FLATBUFFERS_TAG}" \
    https://github.com/google/flatbuffers.git "${WORK_DIR}/flatbuffers"
cp -r "${WORK_DIR}/flatbuffers/include/flatbuffers" \
      "${REPO}/src/core/utility/foxglove/autogenerated_flatbuffers/"

echo "[2/4] Rust toolchain (>= 1.83)"
if ! command -v cargo >/dev/null 2>&1; then
    curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --profile minimal
fi
# shellcheck disable=SC1091
[[ -f "${CARGO_HOME:-$HOME/.cargo}/env" ]] && source "${CARGO_HOME:-$HOME/.cargo}/env"

echo "[3/4] build libfoxglove.a (foxglove-sdk ${FOXGLOVE_SDK_COMMIT:0:8})"
git clone --depth 1 https://github.com/foxglove/foxglove-sdk.git "${WORK_DIR}/foxglove-sdk"
git -C "${WORK_DIR}/foxglove-sdk" fetch --unshallow
git -C "${WORK_DIR}/foxglove-sdk" checkout -f "${FOXGLOVE_SDK_COMMIT}"
export BINDGEN_EXTRA_CLANG_ARGS="-I$(gcc -print-file-name=include)"
cargo build --release --manifest-path "${WORK_DIR}/foxglove-sdk/Cargo.toml" -p foxglove_c

echo "[4/4] install libfoxglove.a"
mkdir -p "${REPO}/src/core/utility/foxglove/lib"
cp "${WORK_DIR}/foxglove-sdk/target/release/libfoxglove.a" \
   "${REPO}/src/core/utility/foxglove/lib/libfoxglove.a"
ls -lh "${REPO}/src/core/utility/foxglove/lib/libfoxglove.a"

echo "done. next: cmake -S \"${REPO}\" -B \"${REPO}/build\" -DCMAKE_BUILD_TYPE=Release && ./run_demo.sh"
```
