# RP-26Rune 部署指南（git clone → run_demo）

> 上游仓库缺少两个**编译期依赖**：flatbuffers `25.2.10` 头文件、Foxglove C 静态库
> `libfoxglove.a`（仓库只含 C++ 包装层）。`scripts/setup_foxglove_deps.sh` 自动补齐，
> 不需要手工下载。

## 1. 完整部署命令

```bash
# 1) 克隆
git clone <仓库地址> && cd RP-26Rune

# 2) 系统依赖：OpenCV / Eigen / Ceres / Boost / glog / OpenVINO / Sophus（需要 sudo，5–15 分钟）
./scripts/setup_ubuntu22.sh

# 3) Foxglove 编译依赖：flatbuffers 头文件 + libfoxglove.a（首次 3–6 分钟，需联网）
./scripts/setup_foxglove_deps.sh

# 4) 构建并运行
./run_demo.sh              # 有桌面：显示 OpenCV 调试窗口
./run_demo.sh --headless   # 无桌面 / SSH
```

第 4 步等价于手动执行：

```bash
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build --parallel "$(nproc)"
./output/app
```

`Ctrl+C` 退出（演示视频默认循环播放）。

## 2. 跑通判据

启动约 30 秒内应出现以下日志（glog 同时输出到终端）：

```text
[NNDetector] initialized OpenVINO rune model; ... model-0624.onnx, device=CPU, input=640x480
context构建完成
[ReceiveDecoder] simulated ECS enabled: mode=BigRune, color=Red, ready=1, period_ms=20
[CameraManager] replay opened: .../big_rune_demo.mp4, fps=30, loop=1
[TrackerManager] 26-style TFTree initialized; camera_offset_m=(-0.1285, 0, 0.0441)
[BigRunePhaseMotionFilter]顺时针        ← 全链路打通
```

数据链路：`ReceiveDecoder(模拟电控) → CameraManager(视频回放) → NNDetector(OpenVINO 五关键点)
→ TrackerManager(power_rune) → PlannerControl(火控) → SendEncoder(模拟发送)`。

可视化：OpenCV 窗口 `RuneDemo/EndToEnd`，以及 [config/power_rune.json](config/power_rune.json)
中 `debug` 开关控制的 `PowerRune/*` 窗口和 Foxglove WebSocket。

## 3. 两个脚本

| 脚本 | 作用 | 参数 | 依赖 |
| --- | --- | --- | --- |
| `scripts/setup_ubuntu22.sh` | 安装 apt 依赖 + OpenVINO + Sophus，并校验环境与随仓资源 | `--check` 只检查；`OPENVINO_VERSION`、`SOPHUS_VERSION` 可覆盖版本 | sudo、联网 |
| `scripts/setup_foxglove_deps.sh` | 补 flatbuffers 头文件与 `libfoxglove.a`，已就绪则跳过 | `--force` 强制重建 `.a`；`--check` 只校验（不联网、不改文件） | 联网、Rust（缺失时自动装） |

## 4. 版本约束

| 组件 | 必须版本 | 原因 |
| --- | --- | --- |
| flatbuffers | `25.2.10` | 43 个 `*_generated.h` 的 `static_assert`；apt 的 2.0.0 不兼容 |
| foxglove-sdk | 快照 `1f42d246` | `libfoxglove.a` 必须与 vendored C++ 包装层同版本（C ABI 结构体布局会变） |
| Rust | ≥ 1.83 | foxglove-sdk `Cargo.toml` 的 `rust-version` |

生成物位置：

- `src/core/utility/foxglove/autogenerated_flatbuffers/flatbuffers/`（头文件，可入库）
- `src/core/utility/foxglove/lib/libfoxglove.a`（约 65 MB，被 `.gitignore` 的 `*.a` 忽略，
  **不入库**，每台机器本地生成）

## 5. 常见问题

| 现象 | 处理 |
| --- | --- |
| `没有规则可制作目标 ".../lib/libfoxglove.a"` | 未执行 `scripts/setup_foxglove_deps.sh` |
| `fatal error: flatbuffers/flatbuffers.h: 没有那个文件` | 同上（缺 flatbuffers 头文件） |
| `static_assert ... Non-compatible flatbuffers version` | 头文件版本不是 25.2.10；删除该目录后重跑脚本 |
| `'stddef.h' file not found`（cargo 构建 `aws-lc-sys` 时） | 脚本已自动设置；手工构建时 `export BINDGEN_EXTRA_CLANG_ARGS="-I$(gcc -print-file-name=include)"` |
| 脚本卡在 `Installing the Rust toolchain` 很久 | `static.rust-lang.org` 慢/不可达。先设镜像再重跑：`export RUSTUP_DIST_SERVER=https://mirrors.tuna.tsinghua.edu.cn/rustup RUSTUP_UPDATE_ROOT=https://mirrors.tuna.tsinghua.edu.cn/rustup/rustup`（若已装好 Rust 则脚本会跳过该步） |
| `error inheriting <dep> from workspace root manifest` | foxglove-sdk 源码树不完整；删掉后重跑脚本 |
| CMake 找不到 OpenVINO | 跑 `setup_ubuntu22.sh`，或 `-DOpenVINO_DIR=/opt/intel/openvino_2024.6.0/runtime/cmake` |
| `[glog] failed to create log dir: "../logs"` | 非致命。glog 日志目录固定为进程 CWD 的上一级，只读时告警但仍打印到终端 |
| `imshow` 报错 / 无窗口 | 用 `./run_demo.sh --headless` |
| 构建 OOM | 降低并行度：`cmake --build build --parallel 4` |

## 6. 不使用脚本时的手工步骤

```bash
# A. flatbuffers 25.2.10 头文件
git clone --depth 1 --branch v25.2.10 https://github.com/google/flatbuffers.git /tmp/flatbuffers
rm -rf src/core/utility/foxglove/autogenerated_flatbuffers/flatbuffers
cp -r /tmp/flatbuffers/include/flatbuffers src/core/utility/foxglove/autogenerated_flatbuffers/

# B. libfoxglove.a（需 Rust ≥ 1.83）
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --profile minimal
source "$HOME/.cargo/env"
git clone --depth 1 https://github.com/foxglove/foxglove-sdk.git /tmp/foxglove-sdk
git -C /tmp/foxglove-sdk fetch --unshallow
git -C /tmp/foxglove-sdk checkout 1f42d24640dc22ced5d21d19c971b448e750d274
export BINDGEN_EXTRA_CLANG_ARGS="-I$(gcc -print-file-name=include)"
cargo build --release --manifest-path /tmp/foxglove-sdk/Cargo.toml -p foxglove_c
mkdir -p src/core/utility/foxglove/lib
cp /tmp/foxglove-sdk/target/release/libfoxglove.a src/core/utility/foxglove/lib/

# 校验
./scripts/setup_foxglove_deps.sh --check
```

## 7. 上游更新了 Foxglove 包装层时

`libfoxglove.a` 的快照 commit 需重新确定：在 foxglove-sdk 历史中找到与
`src/core/utility/foxglove/src/` **逐文件一致**的提交，更新 `scripts/setup_foxglove_deps.sh`
里的 `FOXGLOVE_SDK_COMMIT`（脚本运行时会打印不匹配警告）。

```bash
git clone --depth 1 https://github.com/foxglove/foxglove-sdk.git /tmp/foxglove-sdk
cd /tmp/foxglove-sdk && git fetch --unshallow
git log --format="%h %ad %s" --date=short -- cpp/foxglove/src/cloud_sink.cpp | head   # 缩小候选范围
git archive <sha> cpp/foxglove/src | tar -x -C /tmp/check
diff -r /tmp/check/cpp/foxglove/src <仓库>/src/core/utility/foxglove/src && echo MATCH
```
